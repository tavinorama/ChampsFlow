# Sample deliverable — The Get-Cited Kit ($29)

> This is exactly what a buyer receives for **$29** (one-time, no subscription).
> Generated by `packages/llm/src/kit-deliverable.ts` for a sample brand
> (**Acme CRM**, category "CRM software") in **mock mode**. In production, with a
> provider key set, the draft bodies are written in full by the LLM — the
> `[PLACEHOLDER]` markers below are the no-key fallback only.
>
> Regenerate any time: `npx tsx scripts/gen-kit-example.ts`

---

## 1. Your TrustIndex Score

**Overall: 75 / 100**

| Vector | Score |
|---|---|
| Brand (entity, citations, E-E-A-T) | 72 |
| Performance (schema, crawler access, share of voice, AIO) | 66 |
| AI (citation rate, position, sentiment) | 87 |

Across **30 buyer-intent probes** on 5 engines (Anthropic, OpenAI, Gemini, Perplexity, Google AIO), the brand was cited in **23**.

## 2. Your top 3 "get cited" fixes (prioritized)

**① Allow the major AI crawlers in robots.txt** · *performance · effort low · impact high*
> Gap: one or more AI crawlers (GPTBot, ClaudeBot, PerplexityBot) are blocked in robots.txt. Engines can't read your site.

**② Add Organization, FAQPage, and Article schema to key pages** · *performance · effort medium · impact high*
> Gap: limited schema.org structured data, so AI engines can't easily parse what you offer.

**③ Establish presence on Reddit, LinkedIn, G2** · *brand · effort medium · impact high*
> Gap: absent from the high-authority sources AI cites most (Reddit #1, LinkedIn #2, G2).

## 3. Three ready-to-publish drafts (with schema.org)

### Blog — "Allow the major AI crawlers in robots.txt so engines can read your site"
```
# Allow the major AI crawlers in robots.txt so engines can read your site
## Overview
[in live mode: 2–3 sentences framing the problem for CRM software buyers]
## What the data shows
[in live mode: a statistic or benchmark]
## How Acme CRM approaches it
[in live mode: your specific, sourced method]
## Key takeaways
- …
[Internal link: related service page]
```
*Includes `Article` schema.org JSON-LD ready to paste.*

### LinkedIn
```
Add Organization, FAQPage, and Article schema to your key pages.
At Acme CRM, here's what matters for CRM software: [live: 1–2 data points].
Why it matters: [live: the concrete outcome]. #CRMsoftware #GEO
```

### FAQ (with `FAQPage` schema)
```
**What should you know about establishing presence on Reddit, LinkedIn, G2?**
Acme CRM approaches this with [live: your method]. Key facts: [live: 2–3 points].
```
*Includes `FAQPage` schema.org JSON-LD ready to paste.*

## 4. Where to publish (checklist)
- [ ] Blog post → publish on your website (/blog) and link it from your homepage.
- [ ] LinkedIn post → publish from your company LinkedIn page (the #2 source AI cites).
- [ ] FAQ entry → add to your site's FAQ page **with** the schema.org markup.
- [ ] Create a Wikidata entity for your brand so AI recognizes you as a distinct entity.
- [ ] Re-run your free AI Visibility Test in ~30 days to see movement.

---

### Notes for the founder
- The Kit reuses the **same engine** as the full audit (probes → 3-vector score → strategy → content), but inline & tenant-free for a pre-account buyer — no DB, no subscription.
- **Mock vs live:** with no provider key it degrades to deterministic rules output (the `[PLACEHOLDER]`s). With `ANTHROPIC_API_KEY` (or another provider) set, the 3 drafts are fully written. Cost per Kit ≈ $0.60–$2.00 in live mode (see `docs/COST-MODEL.md`).
- Possible enrichment (not built): a written executive-summary / "why you're invisible" narrative at the top to make the report feel more premium. Say the word and I'll add it.
